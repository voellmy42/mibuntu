{
    "name": "mibuntu-root",
    "version": "1.0.0",
    "description": "Root scripts for Mibuntu",
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "deploy": "firebase deploy"
    }
}